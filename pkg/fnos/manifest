appname         = fn-docker-desk
version         = 1.2.0
display_name    = 飞牛桌面图标
platform        = all
maintainer      = 胖啥胖
maintainer_url  = https://club.fnnas.com
distributor     = 胖啥胖
distributor_url = https://club.fnnas.com
desktop_uidir   = ui
desktop_applaunchname = fn-docker-desk.Application
service_port    = 5558
checkport       = false
ctl_stop        = true
install_type    = root
disable_authorization_path = true
os_min_version  = 1.1.8
desc            = 把 Docker 容器应用一键添加到飞牛桌面。可视化添加/移除桌面图标，支持自定义图标与端口，自动提取容器 favicon。
changelog       = v1.1.18：修复「删除任一图标后其它图标全部损坏（标题/链接/图片丢失，仅显示 D 占位图标）」的致命 bug。根因：remove_icon 末尾的 jq 表达式多写了一个 to_entries，把已经清理过的 [{key,value}] 又转了一次 entries → 输出变成 [{key, value:{key, value:原对象}, 序号}] 三层嵌套，外层丢失了 标题/跳转URL/图片URL 等业务字段，desktop-inject.js 渲染时这些图标标题为空、URL 为空、图片为空，看起来就是「全部损坏」。新添加的图标用的是 . + [...] 直接追加，结构正常，所以表现为「多次添加图标后之前添加的全部损坏」。修复：① 重写 remove_icon 的 jq 表达式：先用 to_entries[] + select 过滤出保留项，再 .value 提取，最后 to_entries + map 重算序号；② 新增 normalize_icons_json 自愈函数，在 init_dirs 中调用，自动检测并修复 v1.1.7 之前 bug 留下的损坏结构（含 key+value 字段的元素自动取 .value，再统一重排序号），升级后首次启动即自动修复现有损坏数据，无需用户手动操作；同步把修复后的数据发布到 DEST_JSON 与 LEGACY_JSON，刷新浏览器即可看到图标恢复正常。v1.1.17：修复 3 处致命/高危漏洞。①【致命-用户精准复现】点击「一键还原初始桌面」后再添加任一容器图标时，之前已删除的全部旧图标会自动复活：根因是 cmd_restore 仅清空 PKG_VAR 官方路径 icons.json，但未清理 v0.2 遗留 /usr/fn-docker-desk 旧路径下的 icons.json/desktop.json/图标图片，也未写入 .migrated_from_usr_fndesk 迁移标记；随后 cmd_add 调用 init_dirs → migrate_legacy_paths 判定「新配置为空且旧路径有数据」→ 把旧路径 icons.json（满是已被用户删除的旧图标）整包迁移回 CONF_JSON，随后 cmd_add 再追加新图标 → 最终旧图标 + 新图标一起出现。修复采用三道防线：(a) cmd_restore 步骤 5 新增清理逻辑：rm -f /usr/fn-docker-desk/icons.json /usr/fn-docker-desk/desktop.json /usr/fn-docker-desk/.restored，清空 /usr/fn-docker-desk/icons 下所有用户图标图片与 backup 目录下用户配置备份；(b) cmd_restore 同步写 PKG_VAR 与 APPDATA_DIR 双路径迁移标记 .migrated_from_usr_fndesk，确保还原后 migrate_legacy_paths 不触发；(c) migrate_legacy_paths 函数开头新增还原态防御：若 PKG_VAR/.restored 或 APPDATA_DIR/.restored 存在则直接 return 0，绝不从旧路径迁回任何数据（还原=用户刻意清空，迁移=复活旧图标）。②【高危】restore_data_from_appdata 中 jq 解析主配置失败时原用 || echo 0 兜底，会误判为「主配置为空」→ 触发从 APPDATA_DIR 备份中恢复旧图标（即使备份里是用户已删除的旧数据），导致 apply 时旧图标复活。修复：jq 解析失败时不再 echo 0 伪装空数组，而是 log_warn + return 0 跳过恢复；新增文件 size<4 辅助判空（只有真正 0 字节/空数组才恢复）；cp 失败时独立打 warn 不再与成功路径混淆。③【日志可观测性】backup_data_to_appdata 内部 3 处 cp 操作原本 || true 静默吞错，与「暂无配置」case 混淆打印相同日志，排查时无法判断备份是否真的失败；修复后对 icons.json/desktop.json/icons 目录 3 处 cp 失败全部独立打 [WARN] 日志，末尾 ok 判定区分"真暂无配置"与"存在但备份失败"。v1.1.16：修复「从任意旧版本（如 1.1.11/1.1.13）无法通过飞牛应用商店一键升级到 1.1.15」的致命问题；应用中心升级流程在 set -e 严格模式下对任一子命令非零 rc 零容忍，而 v1.1.15 中 CLI 脚本共有 3 处 rc 泄漏导致升级整体被判失败：① CLI 主入口 case 未处理 --version/-v/-V 查询版本参数，命中 *）分支→usage 打印 banner 后 exit 1（应用中心任何版本校验都会触发非零退出）；② cmd_list/cmd_ls/cmd_backups/cmd_status 四个只读命令未显式 return 0，while read 循环末尾 EOF 的 read rc=1 泄漏为函数返回值（set -e 下直接视为失败）；③ cmd_ls 中 jq 读取 icons.json 缺失兜底，空配置时 CONF_JSON 不存在导致 jq exit 4→set -e 直接中断。修复：新增 -v/-V/--version 分支并显式 exit 0，所有 cmd_* 只读命令统一末尾 return 0 兜底，cmd_ls jq 加 2>/dev/null || echo 0，main 结尾再补 exit 0 双重保险。v1.1.15：修复「web 界面打开后一直卡在『加载中…』、容器/图标/备份信息不显示」的致命体验问题。根因是多重兜底同时缺失：① Python 端未重写 BaseHTTPRequestHandler.handle_error：当 do_GET/do_POST 内部任一步抛异常（如 docker/ip 命令不可用、icons.json 权限、subprocess 超时等），Python 默认输出 HTML 格式 500 页，而非前端期望的 JSON。② 前端 api() 直接调用 r.json()，遇到 HTML 响应抛 SyntaxError，虽然每个 api 调用有独立 try-catch，但如果浏览器加载 JS 阶段就因 SyntaxWarning 转义异常导致脚本块整体不执行，refreshAll() 永不触发 →「加载中」永久不消失。③ PAGE 字符串拼接 v""" + APP_VERSION + """ 使后半段 HTML/JS 退化为普通字符串（非 r"""..."""），内含 JS 正则 `/^\[(ERR|FATAL)\]/` 中的 `\[` 触发 Python SyntaxWarning，部分环境下会影响脚本热加载。修复策略：① Handler 新增 handle_error() 重写：捕获所有未处理异常 → 写日志到 /var/log/fn-docker-desk.log → 统一返回 JSON 500（含友好错误摘要），前端永远拿到 JSON 不会 r.json() 崩溃。② PAGE 改为 PAGE_TEMPLATE.replace("__APP_VERSION__", APP_VERSION)，全程 raw string，消除 SyntaxWarning；title 与 term-title 两处版本号统一走占位符替换。③ 前端 api() 改为先 r.text() 再 try JSON.parse(text)，非 JSON 时自动剥离 HTML 标签、截取 200 字摘要并打包成 {ok:false, output:...}，同时对 fetch/读取阶段的网络异常兜底返回结构化错误。④ refreshAll() 外层新增全局 try-catch：对任一致命异常替换所有容器/图标/备份卡片内仍含「加载中」的 DOM，渲染为红色错误提示（含日志路径），不会让用户无限等待。⑤ 三个 render 函数也各自加 try-catch，数据渲染异常不阻塞其他卡片。v1.1.13：在 v1.1.12 双层防御基础上再补两道防线，彻底解决「实际成功，但 web.py subprocess.returncode≠0 判为失败 → [web] 命令失败/前端 toast 红字失败」的 rc 泄漏问题：① 真实生产路径一致性漏洞修复（**潜在图标目录删除高危问题**）：实际 fnOS 上 TRIM_PKGVAR 可能指向 `/usr/local/apps/@appdata/fn-docker-desk`，与 APPDATA_DIR（TRIM_APPDEST_VOL）完全重合，导致 backup_data_to_appdata 里「先 rm -rf APPDATA_DIR/icons 再 cp -rf IMAGE_DIR APPDATA_DIR/icons」= 先删除真正的图标目录再复制不存在的源；同时 `cp CONF_JSON → APPDATA_DIR/icons.json` 变成 self-same-file copy 返回非零。修复：backup_data_to_appdata 内先 readlink -f 归一化源/目标路径，重合时直接跳过复制并视为成功（本来就在持久卷上），避免 self-copy 报错与图标目录被误删。② 尾部 rc 泄漏：`cmd_apply` 的末句原是裸写 `[ "$1" != "--quiet" ] && log_info ...`，当 `apply --quiet` 调用（如 upgrade_callback/apply_init 生命周期内部调用）时条件为假导致整条语句 rc=1，又因为是函数最后一条命令 → 函数返回 rc=1（实际全部成功）；修复改用 `{ [ cond ] && log_info; } || true` 包裹，并给 cmd_add/cmd_add_custom/cmd_remove/cmd_apply 四个写入口函数末显式写 `return 0` 兜底，杜绝任何 rc 污染。v1.1.12：修复「添加/删除/应用配置/一键还原」写操作成功但前端提示「移除失败/添加失败」并把 [INFO] 成功日志拼进失败文案的 bug：根因是脚本启用 set -euo pipefail 后，backup_data_to_appdata 内两处裸 `cp`（持久卷 desktop.json、icons 目录复制）在遇到瞬时文件锁/特殊属性/SELinux 拦截返回非零时，直接导致整个 shell 脚本以 rc≠0 退出；web.py 按 exit code 判定 ok=false 触发失败 toast，而 output 中保留了前几步（remove_icon/publish_json）的 INFO 成功日志，形成误导性的「失败：成功」矛盾文案。修复策略采用双层防御：① 脚本层：publish_json 中首次备份 cp、chmod 644/-R 755 全部加 `2>/dev/null || true`；backup_data_to_appdata 内部把两处 set -e 无保护的 cp 改为 `|| true` 或 if 包裹；同时所有 cmd_add/cmd_add_custom/cmd_remove/cmd_apply 对 backup_data_to_appdata 的调用再叠加一层 `|| true`（与现有 install_persistence 保护方式一致），保证备份失败不影响主操作的成功判定。② 前端层：新增 `failReason(output)` 工具函数，从后端原始 output 中仅抽取 [ERR]/[ERROR]/错误关键词行作为失败文案，无错误行时用清晰兜底文案（含日志路径），彻底避免「移除失败：[INFO] 已移除...」类误导。v1.1.11：修复从飞牛桌面打开管理面板时，「添加/删除/应用配置/一键还原」全部写接口 403「跨站请求已拦截」的问题：由于 fnOS 桌面站点（:80/:443）与本管理面板（manifest 默认 :5558）分属不同端口，原先 CSRF 严格比较 netloc 含端口导致合法调用被误杀；现放宽为仅比较 Origin/Referer 与 Host 头的主机名（hostname，不含端口/协议），同时保留对跨主机（如 evil.com）Origin 的 403 拦截，安全与功能兼得。v1.1.10：修复 usr-local-linker 路径 CLI 在非 root 用户（如 SSH admin）下调用时，log_message 写 /var/log/fn-docker-desk.log 权限不足而向 stderr 泄漏 `Permission denied` 警告的问题：_log_file 先检测主日志可写性，不可写时自动兜底到 PKG_VAR/log/fn-docker-desk.log；并修正 shell 重定向顺序 `{ printf ...; } 2>/dev/null >> file` 保证重定向打开失败时消息不泄漏控制台。v1.1.9：修复 usr-local-linker 命令在非生命周期上下文（SSH 直连或外部调用）独立执行时，因 bash nounset 模式导致 `TRIM_PKGMETA: unbound variable` 崩溃；同步对齐打包路径 usr-local-linker `"bin/fn-docker-desk"`、官方 `/var/apps/{appname}/target|var|etc` 结构与旧路径自动迁移。v1.1.8：修复 HTTP 状态码语义错误：CSRF/10MB/上传超限返回 403/400 而非 200，并修正上传接口异常分支未透传非 2xx 问题。v1.1.7：全部 POST 新增 CSRF 同源校验 + 10MB 请求体上限；修复 SVC_PORT 未定义；banner 版本号统一引用 APP_VERSION。v1.1.6：安全与稳定性修复（降权后失效恢复、URL 协议校验、CORS 收窄、写互斥锁、上传 magic bytes 校验、清理 systemd 死代码等）。v1.1.5：空配置时主动清理已渲染图标。v1.1.4：还原逻辑兼容前端 JS 文件名变化并提供备份兜底。v1.1.3：按官方 fnpack 规范移除多余 checksum 字段、对齐 os_min_version/platfor。v1.1.2：修复 download_icon 日志污染 stdout。v1.1.1：差异渲染修复桌面图标闪烁。v1.1.0：nginx reload 替代 restart 避免连接中断。v1.0.9：面板 JS 属性转义修复。v1.0.8：Web 管理服务恢复 root 运行（解决容器读取与图标创建权限）。v1.0.7：代码质量改进 + 57 个单元测试 + CI。v1.0.6：按 fnpack 标准打包 + 应用中心注册入口。v1.0.5：还原保留本应用图标。v1.0.4：还原防止误杀自身。v1.0.1：图标点击被浏览器拦截修复。
source          = thirdparty
